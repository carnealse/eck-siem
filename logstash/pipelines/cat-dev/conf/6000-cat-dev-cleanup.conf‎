filter {
  mutate {
    id => "6000-cat-dev-cleanup-mutate-2"
    remove_field => [ "[check_json]" ]
  }

  if "_mutate_error" in [tags] and ![error][source_plugin_id] {
    mutate {
      add_field => {
        "[error][source_config]"    => "6000-cat-dev-cleanup.conf"
        "[error][source_plugin_id]" => "6000-cat-dev-cleanup-mutate-2"
      }
    }
  }
}
