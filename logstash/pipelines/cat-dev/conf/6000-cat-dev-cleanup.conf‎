filter {
  mutate {
    id => "6000-cat-dev-cleanup-mutate-01"
    remove_field => [ "[check_json]" ]
  }

  if "_mutate_error" in [tags] {
    mutate {
      add_field => {
        "[error][source_config]"    => "6000-cat-dev-cleanup.conf"
        "[error][source_plugin_id]" => "6000-cat-dev-cleanup-mutate-01"
      }
      add_tag => ["_logstash_failure__6000-cat-dev-cleanup__mutate__6000-cat-dev-cleanup-mutate-01", "_logstash_attributed_mutate_error"]
      remove_tag => ["_mutate_error"]
    }
  }
}
