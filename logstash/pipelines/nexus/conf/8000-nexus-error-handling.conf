filter {
  # json failure tagging
  if [isJson] == "true" {
    if "_jsonparsefailure" in [tags] {
      mutate {
        add_field => { "[error][message]" => "json parsing failed" }
        add_tag   => ["filter_error"]
        id => "8000-nexus-json-error"
      }
    }
  }
}
