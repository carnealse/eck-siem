filter {
  if "_csvparsefailure" in [tags] {
    if ![error][detail] {
      mutate { add_field => { "[error][detail]" => "_csvparsefailure in %{[error][source_pipeline]}" } }
    } else {
      mutate { update => { "[error][detail]" => "%{[error][detail]} | _csvparsefailure in %{[error][source_pipeline]}" } }
    }
  }

  if "_dateparsefailure" in [tags] {
    if ![error][detail] {
      mutate { add_field => { "[error][detail]" => "_dateparsefailure in %{[error][source_pipeline]}" } }
    } else {
      mutate { update => { "[error][detail]" => "%{[error][detail]} | _dateparsefailure in %{[error][source_pipeline]}" } }
    }
  }

  if "_dissectfailure" in [tags] {
    if ![error][detail] {
      mutate { add_field => { "[error][detail]" => "_dissectfailure in %{[error][source_pipeline]}" } }
    } else {
      mutate { update => { "[error][detail]" => "%{[error][detail]} | _dissectfailure in %{[error][source_pipeline]}" } }
    }
  }

  if "_elasticsearch_lookup_failure" in [tags] {
    if ![error][detail] {
      mutate { add_field => { "[error][detail]" => "_elasticsearch_lookup_failure in %{[error][source_pipeline]}" } }
    } else {
      mutate { update => { "[error][detail]" => "%{[error][detail]} | _elasticsearch_lookup_failure in %{[error][source_pipeline]}" } }
    }
  }

  if "_geoip_lookup_failure" in [tags] {
    if ![error][detail] {
      mutate { add_field => { "[error][detail]" => "_geoip_lookup_failure in %{[error][source_pipeline]}" } }
    } else {
      mutate { update => { "[error][detail]" => "%{[error][detail]} | _geoip_lookup_failure in %{[error][source_pipeline]}" } }
    }
  }

  if "_grok_kv_parse_failure" in [tags] {
    if ![error][detail] {
      mutate { add_field => { "[error][detail]" => "_grok_kv_parse_failure in %{[error][source_pipeline]}" } }
    } else {
      mutate { update => { "[error][detail]" => "%{[error][detail]} | _grok_kv_parse_failure in %{[error][source_pipeline]}" } }
    }
  }

  if "_grokparsefailure" in [tags] {
    if ![error][detail] {
      mutate { add_field => { "[error][detail]" => "_grokparsefailure in %{[error][source_pipeline]}" } }
    } else {
      mutate { update => { "[error][detail]" => "%{[error][detail]} | _grokparsefailure in %{[error][source_pipeline]}" } }
    }
  }

  if "_jsonparsefailure" in [tags] {
    if ![error][detail] {
      mutate { add_field => { "[error][detail]" => "_jsonparsefailure in %{[error][source_pipeline]}" } }
    } else {
      mutate { update => { "[error][detail]" => "%{[error][detail]} | _jsonparsefailure in %{[error][source_pipeline]}" } }
    }
  }

  if "_kvparsefailure" in [tags] {
    if ![error][detail] {
      mutate { add_field => { "[error][detail]" => "_kvparsefailure in %{[error][source_pipeline]}" } }
    } else {
      mutate { update => { "[error][detail]" => "%{[error][detail]} | _kvparsefailure in %{[error][source_pipeline]}" } }
    }
  }

  if "_multilineparsefailure" in [tags] {
    if ![error][detail] {
      mutate { add_field => { "[error][detail]" => "_multilineparsefailure in %{[error][source_pipeline]}" } }
    } else {
      mutate { update => { "[error][detail]" => "%{[error][detail]} | _multilineparsefailure in %{[error][source_pipeline]}" } }
    }
  }

  if "_mutate_error" in [tags] {
    if ![error][detail] {
      mutate { add_field => { "[error][detail]" => "_mutate_error in %{[error][source_pipeline]}" } }
    } else {
      mutate { update => { "[error][detail]" => "%{[error][detail]} | _mutate_error in %{[error][source_pipeline]}" } }
    }
  }

  if "_rubyexception" in [tags] {
    if ![error][detail] {
      mutate { add_field => { "[error][detail]" => "_rubyexception in %{[error][source_pipeline]}" } }
    } else {
      mutate { update => { "[error][detail]" => "%{[error][detail]} | _rubyexception in %{[error][source_pipeline]}" } }
    }
  }

  if "_xmlparsefailure" in [tags] {
    if ![error][detail] {
      mutate { add_field => { "[error][detail]" => "_xmlparsefailure in %{[error][source_pipeline]}" } }
    } else {
      mutate { update => { "[error][detail]" => "%{[error][detail]} | _xmlparsefailure in %{[error][source_pipeline]}" } }
    }
  }
}
