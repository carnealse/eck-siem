filter {
  if "_csvparsefailure" in [tags] {
    mutate {
      replace => { 
        "[error][detail]" => "_csvparsefailure in %{[error][source_pipeline]}"
      }
    }
  }

  if "_dateparsefailure" in [tags] {
    mutate {
      replace => { 
        "[error][detail]" => "_dateparsefailure in %{[error][source_pipeline]}"
      }
    }
  }

  if "_dissectfailure" in [tags] {
    mutate {
      replace => { 
        "[error][detail]" => "_dissectfailure in %{[error][source_pipeline]}"
      }
    }
  }

  if "_elasticsearch_lookup_failure" in [tags] {
    mutate {
      replace => { 
        "[error][detail]" => "_elasticsearch_lookup_failure in %{[error][source_pipeline]}"
      }
    }
  }

  if "_geoip_lookup_failure" in [tags] {
    mutate {
      replace => { 
        "[error][detail]" => "_geoip_lookup_failure in %{[error][source_pipeline]}"
      }
    }
  }

  if "_grok_kv_parse_failure" in [tags] {
    mutate {
      replace => { 
        "[error][detail]" => "_grok_kv_parse_failure in %{[error][source_pipeline]}"
      }
    }
  }

  if "_grokparsefailure" in [tags] {
    mutate {
      replace => { 
        "[error][detail]" => "_grokparsefailure in %{[error][source_pipeline]}"
      }
    }
  }

  if "_jsonparsefailure" in [tags] {
    mutate {
      replace => { 
        "[error][detail]" => "_jsonparsefailure in %{[error][source_pipeline]}"
      }
    }
  }

  if "_kvparsefailure" in [tags] {
    mutate {
      replace => { 
        "[error][detail]" => "_kvparsefailure in %{[error][source_pipeline]}"
      }
    }
  }

  if "_multilineparsefailure" in [tags] {
    mutate {
      replace => { 
        "[error][detail]" => "_multilineparsefailure in %{[error][source_pipeline]}"
      }
    }
  }


  if "_rubyexception" in [tags] {
    mutate {
      replace => { 
        "[error][detail]" => "_rubyexception in %{[error][source_pipeline]}"
      }
    }
  }

  if "_xmlparsefailure" in [tags] {
    mutate {
      replace => { 
        "[error][detail]" => "_xmlparsefailure in %{[error][source_pipeline]}"
      }
    }
  }
} 
