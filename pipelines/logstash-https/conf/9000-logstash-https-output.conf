output {
  if [data_stream][dataset] == "gitlab" {
      pipeline { 
        send_to => "gitlab" 
        id => "9000-logstash_https-out-gitlab"
      }
    } else {
      pipeline { 
        send_to => "fallback" 
        id => "9000-logstash_https-out-fallback"
      }
    }
}
