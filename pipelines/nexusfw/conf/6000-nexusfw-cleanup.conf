filter {
  # ---------------------------------------------------------------------------------
  # PHASE 4: FINAL CLEANUP
  # ---------------------------------------------------------------------------------
  mutate {
    remove_field => [ "[@metadata][timestamp_raw]", "message" ]
    id => "6000-nexusfw-final-cleanup"
  }
}
